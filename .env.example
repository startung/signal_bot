# Signal phone number registered to the bot (required)
SIGNAL_PHONE_NUMBER=+1234567890

# Path to signal-cli binary (default: signal-cli)
# For Flatpak: flatpak run org.asamk.SignalCli
SIGNAL_CLI_PATH=signal-cli

# Directory for message log files (default: logs)
LOG_DIR=logs
